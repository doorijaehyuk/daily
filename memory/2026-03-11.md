# 2026-03-11

- 형님이 assistant identity를 확정함: 이름은 김비서, 형님을 정중하고 친근하게 대하며 호칭은 형님, 시그니처 이모지는 🫡.
- git 작성자 정보(작업공간 저장소 기준으로 사용할 값):
  - user.name: doorijaehyuk
  - user.email: doorijaehyuk@gmail.com
- repositories 관련 기록은 daily 메모에 저장하기로 함.
- GitHub 인증 저장 이후, 김비서가 테스트 커밋/푸시를 진행했고 정상 동작을 확인함.
- 테스트 파일은 확인 후 정리했다.
- 앞으로 하는 모든 작업 내용은 daily에 기록한다.
- 필요한 설정 스냅샷은 workspace 안으로 옮겨 기록한다.
- 기록한 내용은 git에 커밋/푸시까지 진행한다.
- Slack 연동 진단 결과: Slack 채널 송신(send)은 정상 동작했고, `#agent` 채널 ID는 `C0ALMS5M588`로 확인됨.
- Slack 수신 문제는 멘션 이벤트가 아예 안 들어오는 게 아니라, 이벤트 처리 시점에 Socket Mode 연결이 끊겨 `client has no active connection` / `Failed to send a WebSocket message as the client is not ready` 에러가 나는 것으로 확인됨.
- 반복 로그로 `A pong wasn't received from the server before the timeout of 5000ms` 가 계속 발생해, 현재 OpenClaw Slack 연동의 핵심 병목은 Socket Mode keepalive/연결 유지 문제로 판단됨.
- 다음으로 의미 있는 진단 방향은 Slack 계정을 하나만 남겨 단일 계정에서도 같은 문제가 재현되는지 확인하는 것.
- 단일 계정 테스트 결과: 박과장 Slack 계정을 비활성화하고 김비서 Slack만 남겨도 동일하게 `A pong wasn't received...` 가 반복되어, 멀티 계정 충돌 문제가 아니라는 점을 확인함.
- 형님 요청에 따라 이후에는 박과장 Slack 계정을 다시 활성화하고, 진단 결과를 snapshots 및 git에 정리하기로 함.
- 2026-03-12에는 형님 요청으로 Slack 연동을 당분간 포기하고, Node 20 관련 흔적 및 Slack 관련 설정/스냅샷을 정리해 Node 22 기본 상태로 되돌리기로 함.
